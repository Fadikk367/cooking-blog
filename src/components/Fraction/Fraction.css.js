import styled from 'styled-components';

export const FractinWrapper = styled.span`
  font-size: 1em;

  span {
    position: relative;
  }
`;

export const Integer = styled.span`
  font-size: 1em;
  margin-right: 5px;
`;

export const Numerator = styled.span`
  top: -0.4em;
  left: 0.1em;
  font-size: 0.7em;
`;

export const FractionLine = styled.span`
  top: 0.1em;
`;

export const Denominator = styled.span`
  top: 0.3em;
  right: 0.2em;
  font-size: 0.7em;
`;